/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com', 'cloudflare-ipfs.com'], // Add other domains as needed
    },
  };
  
  export default nextConfig;
  